# Cloudflare Pages — rewrites (status 200 = proxy interno, sem mudar a URL)

# Home abre o painel
/   /painel-sbs.html   200

# Protege fontes do backend e docs: acesso direto volta para a home (302)
/server/*    /   302
/backend/*   /   302
