#:schema node_modules/wrangler/config-schema.json
# Cloudflare Pages — configuração do projeto SBS Green Seeds
# nodejs_compat: habilita process.env, node:crypto (HMAC do login) etc. no runtime Workers.

name = "painel-sbs"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Site estático na raiz + Pages Functions em /functions
pages_build_output_dir = "."
